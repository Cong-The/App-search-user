class IdUser {
  final String uid;
  IdUser({required this.uid});
}

class IdUserData {
  final String uid, age, name, address, phone, interest;

  IdUserData({
    required this.uid,
    required this.age,
    required this.name,
    required this.address,
    required this.phone,
    required this.interest,
  });
}
